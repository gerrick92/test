# WoW Database Explorer v6 patch

Copy these files over the current repo root.

What this patch changes:

- Fixes image assets so they are real PNG/WebP files in `static/img/`.
- Restores the home page to the Dark Portal direction.
- Home page uses the portal image with three clickable expansion logo areas.
- Removes the visible home-page database info and All Data button.
- Moves database info and the All Data link into Settings.
- Keeps existing DB/search/pagination logic untouched.

Not included yet:

- music per page
- skills/spells/achievements expansion
- deeper Vanilla/TBC/WotLK page redesign

Those are next after this image/home-page pass is tested.
